# contracts/events

JSON Schema definitions for the Kafka event envelope and each topic's
payload, one file per `event_type` + `event_version`. Added starting Phase 2
alongside the transactional outbox implementation. See
`docs/events/event-envelope.md` and `docs/events/topic-catalog.md` (added
when this directory gains content) for the envelope shape and topic list.
